import 'package:get/get.dart';

class SizeApp {
  static double width = Get.size.width;
  static double height = Get.size.height;
  static double smallSize = Get.size.width * 0.0300;
  static double normalSize = Get.size.width * 0.0325;
  static double LargelSize = Get.size.width * 0.0370;
}
